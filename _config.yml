# Site settings
name: "mb21’s blog"
description: "about society, philosophy, design, movies, tech, etc."
baseurl: "/blog"
url: "http://mb21.github.io/blog"
githubUsername: mb21
twitterUsername: mb2100

# Build settings
permalinks: pretty

defaults:
  -
    scope:
      path: ""      # empty string for all files
      type: post    # limit to posts
    values:
      is_post: true # automatically set is_post=true for all posts

# Plugins
pandoc:
  skip: false
  impose: false
  output: ./tmp
  flags:
  site_flags:
  outputs:
    html:
